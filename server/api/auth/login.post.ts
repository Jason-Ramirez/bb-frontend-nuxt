
export default defineEventHandler(async (event) => {
  // @remind
  
  // const
  // getCookie();

  return {success: false, error: 'test'}

  // await $fetch(`${process.env.BB_BACKEND_URL}/sanctum/csrf-cookie`);
  // const user = await useBody(event);
  // const loginData = JSON.stringify(user);
  // const responseJson = await $fetch(`${process.env.BB_BACKEND_URL}/login`, {
  //   method: 'POST',
  //   body: loginData,
  // });
  // return responseJson;
});